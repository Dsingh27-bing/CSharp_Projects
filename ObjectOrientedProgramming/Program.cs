using System.Data.Common;

namespace ObjectOrientedProgramming;

class Program
{
    static void Main(string[] args)
    {
        // ReverseArray ra = new ReverseArray();
        // Console.WriteLine("Enter the length of the array: ");
        // int input = Convert.ToInt32(Console.ReadLine());
        // ra.GenerateNumbers(input);
        // ra.PrintNumbers();
        // ra.Reverse();
        // ra.PrintNumbers();
        // ClearScreen();

        // Fibonacci fb = new Fibonacci();
        // Console.WriteLine("Enter the index of fibonacci sequence you want to know: ");
        // int index = Convert.ToInt32(Console.ReadLine());
        // Console.WriteLine("Enter the length of fibonacci sequence (Optional): ");
        // if (Console.ReadLine() == "" || Console.ReadLine() == " " || Console.ReadLine() == "0") 
        //     fb.FibonacciSequence(index);
        // else
        // {
        //     int len = Convert.ToInt32(Console.ReadLine());
        //     fb.FibonacciSequence(len, index);
        // }
        //
        // ClearScreen();
        
        

    }
    
    static void ClearScreen()
    {
        System.Threading.Thread.Sleep(2000);
        Console.Clear();
    }
}